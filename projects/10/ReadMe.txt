
在 main function內去，判斷讀進來的是 folder還是單檔，

有個tokenizer的 func 去處理，每個輸入的 jack file，

所以最前面，要有個前處理，把要輸入的，jack file給列出來，

這塊先不用理。

先實作一個function，把單檔的jack file 能處理好，

要放在不同的folder，output的檔案分二階段，

第一階是token , 檔名加T.xml , 檔名.xml。


